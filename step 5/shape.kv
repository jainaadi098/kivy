<MyShape>:
    canvas:
        # Set the color to Blue (R, G, B, A)
        Color:
            rgba: 0, 0, 1, 1 
        
        # Draw a Rectangle
        Rectangle:
            # We want it in the middle (approx)
            pos: 380, 500
            size: 300, 200